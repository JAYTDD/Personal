# Netlify Function only — never prefix with VITE_
# Configure in Netlify UI → Site configuration → Environment variables,
# or put a local value in `.env` for `npm run dev:netlify`.
#
# classic PAT: read:user (+ repo if private contribs needed)
# fine-grained: user JAYTDD, Metadata: Read
GITHUB_TOKEN=
