# Order matters: API proxy first, then SPA fallback.
# Netlify merges this with netlify.toml; keep both sources consistent.
/api/github/*  /.netlify/functions/:splat  200
/*             /index.html                 200
